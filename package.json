{
  "name": "@lost-and-found/api",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Lost and Found platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "npx openapi-typescript http://localhost:8082/v3/api-docs -o ./src/index.ts --root-types --root-types-no-schema-prefix --enum",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
